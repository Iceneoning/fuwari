# 性能优化专题

本目录专门存放博客的性能分析、优化方案和复测记录。这里的内容默认是工作文档；只有完成实施并通过复测后，才整理到 `src/content/posts/` 作为正式文章发布。

## 当前基线

本轮问题来自 2026-08 的 Lighthouse「数据分析」截图：

- 「渲染阻塞请求」预计可缩短约 `1,710 ms`，报告中列出了 6 个首屏 CSS 请求。
- 「强制自动重排」被标记为待优化项，但截图未展开调用栈和耗时明细。
- 「LCP 细分」被标记为待优化项，但截图未展开 LCP 元素和各阶段耗时。

截图中的 CSS 请求合计约 `49.9 KiB`。文件名包含构建哈希，后续构建可能变化，因此以下数据只作为本轮基线：

| CSS 资源 | 传输大小 | 截图耗时 |
| --- | ---: | ---: |
| `friends.*.css` | 2.1 KiB | 1,670 ms |
| `about.*.css` | 5.3 KiB | 1,670 ms |
| `_page_.*.css` | 21.0 KiB | 1,750 ms |
| `_page_.*.css` | 16.4 KiB | 1,710 ms |
| `Layout.*.css` | 3.2 KiB | 1,590 ms |
| `photoswipe.*.css` | 1.9 KiB | 1,670 ms |

## 帖子目录

| 序号 | 主题 | 状态 | 文档 |
| --- | --- | --- | --- |
| 01 | 渲染阻塞 CSS 请求 | 已实施，待线上 Lighthouse 复测 | [减少渲染阻塞 CSS](./01-render-blocking-css.md) |
| 02 | 强制自动重排 | 已实施并完成本地烟雾测试 | [定位并减少强制自动重排](./02-forced-reflow.md) |
| 03 | LCP 细分 | 已优化渲染延迟路径，待线上四阶段复测 | [按阶段优化 LCP](./03-lcp-breakdown.md) |

## 本轮实施摘要

- 将 `ImageWrapper.astro` 的全源码 glob 收窄为图片扩展名，修复所有路由样式被错误纳入每个页面的问题。
- 显式拆分公共样式、Markdown 样式和路由样式；PhotoSwipe CSS 改为有图片时按需注入。
- Roboto 仅保留实际使用的 Latin 子集。
- 删除首屏同步初始化的 OverlayScrollbars，改用带主题色的原生滚动条。
- 合并滚动事件到动画帧，避免同一帧交错读写布局；TOC 改为先测量、后统一写入。
- 鼠标特效在空闲时停止 `requestAnimationFrame` 循环。

生产构建中，友链页首屏 CSS 从 6 个请求降为 2 个；gzip 估算由 `48,534 B` 降为 `14,922 B`。以上是构建产物对比，不替代部署环境中的 Lighthouse 复测。

## 记录约定

每篇优化记录至少包含以下内容：

1. 问题证据和可复现条件。
2. 根因，而不只记录 Lighthouse 提示。
3. 实施方案、涉及文件和风险。
4. 优化前后数据，优先记录至少 3 次测试的中位数。
5. 桌面端、移动端以及浅色/深色主题的回归结果。

测试环境变化会显著影响结果。复测时需要固定页面 URL、构建版本、设备模拟、网络与 CPU 节流条件，并记录 Lighthouse 版本。
